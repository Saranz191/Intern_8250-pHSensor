# งานฝึกปฏิบัติ: เฟิร์มแวร์ pH สี่ช่องสัญญาณ

เป้าหมายฮาร์ดแวร์ของงานนี้คือ NUCLEO-L432KC, SIC8250 และช่องวัด pH สี่ช่อง
ที่กำหนดหมายเลข 0–3

## สิ่งที่ต้องศึกษาและออกแบบ

อ่านไฟล์ `SIC8250_pH_OCP_Flow.drawio` แล้วสกัด state, event, guard, action และ
transition ออกมาเป็นแบบออกแบบของคุณเองก่อนเริ่มเขียนโค้ด รวมถึงอธิบายตาราง
transition ที่เลือกใช้ การออกแบบต้องไม่ผูกกับการหน่วงเวลาแบบบล็อก และต้องรักษา
หมายเลขช่องไว้กับทุกตัวอย่างและทุกข้อผิดพลาด

## งานที่ต้องดำเนินการ

- ตั้งค่า CubeMX จากการต่อสายจริงของบอร์ดและอุปกรณ์
- พัฒนา BSP สำหรับการเข้าถึงฮาร์ดแวร์ และทำให้การเรียก HAL อยู่ใน BSP เท่านั้น
- พัฒนา operation ของ SIC8250 โดยแยกจากรายละเอียดของบอร์ด
- พัฒนา FSM แบบไม่บล็อกตาม flow ที่วิเคราะห์ไว้
- ออกแบบการจัดตารางทำงานสำหรับทั้งสี่ช่อง และทำให้ operation ของช่องเดียวใช้ซ้ำได้
- เพิ่ม timeout และ recovery ที่ปลอดภัย
- แปลงค่าที่อ่านได้เป็นค่าที่ต้องรายงาน และส่งผลลัพธ์ที่ติดป้ายหมายเลขช่องเสมอ

ลำดับการตั้งค่าอุปกรณ์ต้องออกแบบให้ใช้ซ้ำได้ ห้ามคัดลอกชุดคำสั่งเดิมสี่ครั้งสำหรับ
แต่ละช่อง ห้ามใช้การ polling ADC แบบบล็อก

## การส่งงาน

สร้าง branch ชื่อ `feature/<short-description>` จาก `main` ล่าสุด แล้วส่งงานเป็น
pull request ห้าม commit ตรงไปยัง `main` โดย pull request ต้องมีคำอธิบายสถาปัตยกรรม
แบบสั้น และหลักฐานว่า build ใน STM32CubeIDE สำเร็จ
